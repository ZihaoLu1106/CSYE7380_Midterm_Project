tensorflow==2.17.0         # For building and training your LSTM model
tensorflow-hub==0.14.0     # Optional if using TensorFlow Hub for feature extraction
numpy==1.24.3              # For handling numerical operations
pandas==2.0.3              # For working with data (if needed)
scikit-learn==1.3.0        # For train-test splitting
matplotlib==3.7.2          # For plotting if required
nltk==3.8.1                # For evaluating with BLEU score
Pillow==9.5.0              # For image preprocessing
kaggle==1.5.13             # For downloading datasets
keras==2.12.0              # For building models (comes bundled with TensorFlow)
h5py==3.9.0                # For saving and loading model weights